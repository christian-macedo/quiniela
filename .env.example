# =============================================================================
# Local Supabase (recommended for development)
# =============================================================================
# Run `npm run supabase:start` then `npm run supabase:status -- -o env`
# Copy the ANON_KEY and SERVICE_ROLE_KEY JWT values from the env output.
#
# NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# NEXT_PUBLIC_SUPABASE_ANON_KEY=<ANON_KEY from supabase status -o env>
# SUPABASE_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY from supabase status -o env>
# NEXT_PUBLIC_RP_NAME="Quiniela Dev"
# NEXT_PUBLIC_RP_ID=localhost
# NEXT_PUBLIC_RP_ORIGIN=http://localhost:3000

# =============================================================================
# Cloud Supabase
# =============================================================================
# Get these values from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (required for passkey authentication)
# This is used server-side only to create sessions after passkey verification
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# WebAuthn / Passkey Configuration
# Human-readable name for your site
NEXT_PUBLIC_RP_NAME="Quiniela Predictions"

# Domain name (use "localhost" for development, your domain for production)
NEXT_PUBLIC_RP_ID=localhost

# Full origin URL (http://localhost:3000 for dev, https://yourdomain.com for prod)
NEXT_PUBLIC_RP_ORIGIN=http://localhost:3000
